import type { ReactNode } from "react";

// =====================================================
// GLOBAL STYLE TYPES - Tất cả types cho GlobalStyle component
// =====================================================

/** Props cho GlobalStyle component */
interface GlobalStyleProps {
  /** React children */
  children: ReactNode;
}

export type { GlobalStyleProps };
